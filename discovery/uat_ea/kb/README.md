# Legacy KB archive (JSON)

**Status:** ARCHIVED — do not add new documents here.

The canonical Application Knowledge Base format is:

- `../flows/*.yaml` — flow-centric SME format (see `../KB_FORMAT.md`)
- `../capabilities.yaml` — capability → flow map

This folder retains **64 JSON documents** from automated crawl and browser recordings (Sep 2026). Use as reference when enriching DRAFT YAML flows.

Migration target: all active flows → `../flows/` at READY standard.
