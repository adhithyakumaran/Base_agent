/**
 * @deprecated Legacy single-page console shell. Canonical UI is ScoutApp (`components/scout-app.tsx`).
 * Retained for reference and gradual migration; not mounted by `app/page.tsx`.
 */
"use client";

export { QaConsole } from "@/components/qa-console-legacy";
